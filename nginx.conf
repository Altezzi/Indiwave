events {
    worker_connections 1024;
}

http {
    upstream indiwave {
        server indiwave-app:3000;
    }

    # HTTP server (temporary - will add HTTPS later)
    server {
        listen 80;
        listen 2222;
        listen 443;
        server_name indiwave.io www.indiwave.io localhost;

        # Security headers
        add_header X-Frame-Options DENY;
        add_header X-Content-Type-Options nosniff;
        add_header X-XSS-Protection "1; mode=block";

        # Proxy settings
        location / {
            proxy_pass http://indiwave;
            proxy_http_version 1.1;
            proxy_set_header Upgrade $http_upgrade;
            proxy_set_header Connection 'upgrade';
            proxy_set_header Host $host;
            proxy_set_header X-Real-IP $remote_addr;
            proxy_set_header X-Forwarded-For $proxy_add_x_forwarded_for;
            proxy_set_header X-Forwarded-Proto $scheme;
            proxy_set_header X-Forwarded-Host $host;
            proxy_set_header X-Forwarded-Port $server_port;
            proxy_cache_bypass $http_upgrade;
            proxy_read_timeout 86400;
            
            # Cookie and session handling
            proxy_cookie_domain localhost indiwave.io;
            proxy_cookie_domain localhost www.indiwave.io;
            proxy_cookie_path / /;
        }

        # Health check endpoint
        location /api/health {
            proxy_pass http://indiwave/api/health;
            proxy_set_header Host $host;
            proxy_set_header X-Real-IP $remote_addr;
            proxy_set_header X-Forwarded-For $proxy_add_x_forwarded_for;
            proxy_set_header X-Forwarded-Proto $scheme;
        }
    }
}
